# DevSafe GUI — Solution & Architecture

Version: 1.0  
Status: IMPLEMENTABLE  
Related:
- 01_TZ.md
- 02_TESTS.md  
Owner: Viktor Kulichenko  
Date: 2026-01-01

---

## 1. Цель реализации

Реализовать desktop-приложение **DevSafe GUI**, которое обеспечивает:

- автоматические Git-коммиты,
- автоматический push в GitHub,
- локальное резервное копирование проекта,
- управление до 5 проектов,
- защиту от потери кода,

в соответствии с утверждённым ТЗ и тест-кейсами.

---

## 2. Общая архитектура

### 2.1 Архитектурный принцип

> **Single Active Project, Multi-Project Registry**

- в системе может быть до 5 проектов
- одновременно активен только 1 проект
- для активного проекта работает:
  - file watcher
  - git engine
  - backup engine

---

### 2.2 Архитектурная схема (логическая)

┌────────────────────┐
│ GUI Layer │
│ (Tkinter Window) │
└─────────┬──────────┘
│
▼
┌────────────────────┐
│ Project Manager │
│ (config / state) │
└─────────┬──────────┘
│
▼
┌────────────────────┐
│ Active Project │
│ │
│ ┌──────────────┐ │
│ │ File Watcher │◀─┼── filesystem events
│ └──────┬───────┘ │
│ ▼ │
│ ┌──────────────┐ │
│ │ Git Manager │──┼── git commit / push
│ └──────┬───────┘ │
│ ▼ │
│ ┌──────────────┐ │
│ │ Backup Engine│──┼── local mirror
│ └──────────────┘ │
└────────────────────┘


---

## 3. Структура проекта



devsafe/
├── app.py
├── core/
│ ├── project_manager.py
│ ├── watcher.py
│ ├── git_manager.py
│ ├── backup_manager.py
│ └── scheduler.py
│
├── ui/
│ └── main_window.py
│
├── data/
│ └── devsafe.json
│
├── docs/
│ └── devsafe/
│ ├── 01_TZ.md
│ ├── 02_TESTS.md
│ └── 03_SOLUTION.md
│
└── requirements.txt


---

## 4. Основные компоненты

### 4.1 GUI Layer

**Ответственность:**
- отображение списка проектов
- выбор активного проекта
- запуск / остановка защиты
- отображение статуса и логов

**Технология:**  
- `tkinter`

GUI **не содержит бизнес-логики**.

---

### 4.2 Project Manager

**Ответственность:**
- хранение конфигурации проектов
- контроль лимита (≤5)
- активация / деактивация проектов
- восстановление состояния при перезапуске

**Хранилище:**


data/devsafe.json


---

### 4.3 File Watcher

**Ответственность:**
- отслеживание изменений файлов активного проекта
- фильтрация системных директорий
- агрегация событий (debounce)

**Технология:**
- `watchdog`

---

### 4.4 Git Manager

**Ответственность:**
- `git add`
- `git commit`
- `git push`

**Особенности:**
- уважает `.gitignore`
- не блокирует работу при ошибке push
- всегда создаёт локальный commit

---

### 4.5 Backup Engine

**Ответственность:**
- зеркалирование проекта в локальную папку
- исключение `.git`, `.venv`, кешей
- поддержание актуального состояния

**Технология:**
- `robocopy` (Windows)
- либо `shutil` (fallback)

---

## 5. Поток выполнения (runtime flow)

### 5.1 Нормальный сценарий



File change
↓
Watcher detects
↓
Debounce timeout
↓
Git commit
↓
Git push
↓
Local backup


---

### 5.2 Сценарий отказа GitHub



File change
↓
Git commit (OK)
↓
Git push (FAIL)
↓
Log error
↓
Local backup (OK)


---

## 6. Соответствие тестам

| Test ID | Реализация |
|------|-----------|
| AT-01 | Project Manager |
| AT-02 | MAX_PROJECTS = 5 |
| AT-03 | Single Active Watcher |
| WT-01 | watchdog observer |
| GT-01 | git_manager |
| BT-01 | backup_manager |
| FR-01 | config restore |

---

## 7. Расширяемость

Архитектура допускает добавление:

- tray-режима
- нескольких активных проектов
- шифрования резервов
- cloud storage
- enterprise-версии (MindForge Guard)

без изменения базовой логики.

---

## 8. Ограничения реализации (осознанные)

- Windows only
- один активный проект
- локальное хранение конфигурации
- без сетевых FS

---

## 9. Контрольная точка

✔️ ТЗ зафиксировано  
✔️ Тесты утверждены  
✔️ Архитектура описана  

👉 **Разрешено писать код**

---

## 10. Следующий шаг

- реализация по модулям
- сначала `project_manager`
- затем `watcher`
- затем `git_manager`
- затем `backup_engine`
- затем GUI
